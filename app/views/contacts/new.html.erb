<div class="container">
    <div class="wrapper col-md-6 col-md-offset-3 col-sm-10 col-sm-offset-1">
        <p id="notice"><%= notice %></p>
        <h1>お問い合わせ</h1>
            <p>以下のお問い合わせフォームへ入力したあと、<br>「入力内容を確認する」ボタンを押下してください。</p>
    
        <%= form_for(@contact, url:confirm_contacts_path) do |f| %>
            <!-- バリデーションエラーの件数表示するよ dive02-->
            <% if @contact.errors.any? %>
                <div id="error_explanation">
                            <!--@contact.errorsはバリデーションした際のエラー情報です dive02 -->
                            <!--@full_messagesは複数のエラーメッセージを表示します dive02 -->
                    <h2><%= @contact.errors.count %>件のエラーがあります。</h2>
                    <ul>
                        <% @contact.errors.full_messages.each do |msg| %>
                        <li><%= msg %></li>
                        <% end %>
                    </ul>
                </div>
            <% end %>
        
            <!-- <%= f.label :name %> -->
            <p>名前(お問い合わせ件名)：<%= f.text_field :name %></p>
            <!-- <%= f.label :email %> -->
            <p>メールアドレス：<%= f.text_field :email %></p>
            <!-- <%= f.label :content %> -->
            <p>お問い合わせ内容：<%= f.text_field :content %></p>
            <%= f.submit "入力内容を確認する", class: 'btn btn-default btn-md btn-success' %>    <!-- # dive4課題でbootstrap化したよ。 -->
        <% end %>
    </div>
</div>